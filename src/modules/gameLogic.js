import { Player } from './player';
import { GameBoard } from './gameBoard';
import { Ship } from './ship';

const gameLoop = (playerName) => {
  const playerOne = Player(playerName);
  const playerOneGameBoard = GameBoard();
};
