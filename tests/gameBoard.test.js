import { gameBoard } from '../src/modules/gameBoard';
import { ship } from '../src/modules/ship';

const testShip = ship(4);
const testGameBoard = gameBoard();

describe('is gameboard operational', () => {
  test('check if receiveAttack');
});
